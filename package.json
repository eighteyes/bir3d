{
  "name": "vector-system",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:gpu": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
